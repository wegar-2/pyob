# pyob

An implementation of order book in Python.

### Note on Installation of `llist`

You can specify path to `gcc` used during installation as follows:
```commandline
CC=/.../gcc-16 uv pip install llist
```
